{
  "name": "backend",
  "private": true,
  "version": "0.0.1",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --env-file-if-exists=../.env --respawn --transpile-only src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file-if-exists=../.env dist/server.js"
  },
  "dependencies": {
    "@mysten/sui": "^1.3.0",
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.6",
    "express": "^4.19.2",
    "jose": "^6.2.11"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
